<!doctype html><html lang="pt-PT"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Resultados de exames · NOSH CRM Saúde</title><link rel="stylesheet" href="{{ asset('css/nosh-patient-v070.css') }}?v=0.7.0"></head><body class="portal-body"><header class="portal-header"><a href="{{ route('patient.dashboard') }}" class="patient-brand">✚ <strong>NOSH CRM Saúde</strong></a><nav><a href="{{ route('patient.dashboard') }}">Visão geral</a><a href="{{ route('patient.appointments') }}">Consultas</a><a class="active" href="{{ route('patient.exams') }}">Resultados de exames</a><a href="{{ route('patient.security') }}">Segurança</a></nav><form method="post" action="{{ route('patient.logout') }}">@csrf<button>Sair</button></form></header><main class="portal-main"><section class="portal-page-head"><span>DOCUMENTOS LIBERADOS</span><h1>Resultados de exames</h1><p>Apenas exames já liberados pela clínica aparecem nesta área.</p></section><div class="exam-grid">@forelse($exams as $exam)<article class="exam-card"><div class="exam-icon">↧</div><span class="exam-category">{{ mb_strtoupper($exam->category) }}</span><h3>{{ $exam->title }}</h3><p>{{ $exam->summary ?: 'Resultado disponibilizado pela sua clínica.' }}</p><div class="exam-meta"><span>Data: {{ $exam->exam_date->format('d/m/Y') }}</span>@if($exam->laboratory)<span>{{ $exam->laboratory }}</span>@endif</div><a class="portal-primary inline" href="{{ route('patient.exams.download',$exam) }}">Abrir resultado</a></article>@empty<div class="portal-empty">Nenhum resultado de exame foi liberado para a sua conta.</div>@endforelse</div></main></body></html>
